{
  "name": "voicemail",
  "version": "1.0.0",
  "private": true,
  "description": "Private voicemail with transcripts, running on your own infrastructure",
  "scripts": {
    "setup": "node scripts/install.mjs",
    "test": "npm --prefix worker test"
  },
  "engines": {
    "node": ">=20"
  }
}
